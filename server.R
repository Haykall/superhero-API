# Server File
# server
library(ggplot2)
library(plotly)
library(dplyr)
library(shiny)

# Server used to run the app
server <- function(input, output) {
  # First page output, changes race and color


}